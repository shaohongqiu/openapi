MeSettings:
  type: object
  description: Settings for the authenticated user.
  properties:
    openapi_mode:
      type: string
      enum: [unset, single]
      description: OpenAPI mode for this user.
  required: [openapi_mode]
  example:
    tenancy_mode: single
